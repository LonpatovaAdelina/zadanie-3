#include <iostream>
#include <math.h>
using namespace std;
void somn(int n)
{
	for (int i=2;i<=n;)
	{
		if (n % i == 0)
		{
			cout << i << endl;
			n /= i;
		}
		else
			++i;
	}
}
int main()
{
	int n;
	cin >> n;
	somn(n);
}
